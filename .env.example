# ============================================
# 数据库配置（PostgreSQL）
# ============================================
# 使用 docker-compose.yml 中的 postgres 服务配置
# 默认端口：15432，用户：postgres，密码：postgres，数据库：taskpm
POSTGRES_DSN=postgresql://postgres:postgres@localhost:15432/taskpm?sslmode=disable

# Prisma 使用的数据库连接字符串（兼容 Prisma 格式）
# 如果设置了 POSTGRES_DSN，Prisma 会优先使用它
DATABASE_URL=postgresql://postgres:postgres@localhost:15432/taskpm?sslmode=disable

# ============================================
# Redis 配置
# ============================================
# 使用 docker-compose.yml 中的 redis 服务配置
# 默认端口：16379（映射到容器内的 6379）
REDIS_ADDR=redis://localhost:16379/0

# ============================================
# 后端服务配置
# ============================================
# HTTP 服务器地址
HTTP_ADDR=:28080

# gRPC 服务器地址
GRPC_ADDR=:29090

# ============================================
# 前端配置
# ============================================
# 后端 API 地址（用于 Vite 代理）
VITE_API_URL=http://127.0.0.1:28080

# ============================================
# Worker 配置
# ============================================
# Worker 控制面基础 URL（用于上报任务状态）
BASE_URL=http://127.0.0.1:28080

# Worker 名称（用于标识不同的 worker 实例）
WORKER_NAME=worker-1
