apiVersion: v1
kind: Service
metadata:
  name: {{ include "asynqhub.backend.fullname" . }}
  labels:
    {{- include "asynqhub.labels" . | nindent 4 }}
    app.kubernetes.io/component: backend
spec:
  type: {{ .Values.backend.service.type }}
  ports:
  - name: http
    port: {{ .Values.backend.service.httpPort }}
    targetPort: http
    protocol: TCP
  - name: grpc
    port: {{ .Values.backend.service.grpcPort }}
    targetPort: grpc
    protocol: TCP
  selector:
    {{- include "asynqhub.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: backend
